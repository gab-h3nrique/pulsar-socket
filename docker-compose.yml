
services:

  socket:
    build: "./"
    container_name: pulsar-socket
    restart: always
    volumes:
      - .:/src
    ports:
     - "3001:3001"

  # nginx:
  #   image: nginx:latest
  #   container_name: pulsar-nginx
  #   restart: always
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx/conf.d:/etc/nginx/conf.d
  #   depends_on:
  #     - socket